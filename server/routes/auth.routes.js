const express = require("express");
const router = express.Router({ mergeParams: true});

router.post("/signUp", async () => {

})

router.post("/signInWithPassword", async () => {
    
})

router.post("/token", async () => {
    
})

module.exports = router;